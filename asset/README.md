# How to Compile the SASS
`cd` into this directory, and run `sass --compass ./sass/styles.scss ./css/styles.css`

This project might be converted to use roots later, so nobody needs to remember what commands to use.
